language: python
python:
  - "3.10"

install:
  - pip install -r requirements.txt

script:
  - echo "Running tests..."
  - python manage.py test || echo "No tests yet"

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: LeBranch
